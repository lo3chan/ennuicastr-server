diff --git a/node_modules/nodejs-server-pages/runner.js b/node_modules/nodejs-server-pages/runner.js
index a47a3e9..e3c528e 100644
--- a/node_modules/nodejs-server-pages/runner.js
+++ b/node_modules/nodejs-server-pages/runner.js
@@ -330,10 +330,10 @@ function run(db, params, req, body, res) {
     };

     // Run it
-    func(module).then(() => {
+    func(module).then(async () => {
         clearTimeout(timeout);
-        finish();
-    }).catch((ex) => {
+        await finish();
+    }).catch(async (ex) => {
         clearTimeout(timeout);
         process.send({
             c: "x",
@@ -342,12 +342,12 @@ function run(db, params, req, body, res) {
             e: ex + "\n" + ex.stack
         });
         res.write("ERROR");
-        finish();
+        await finish();
     });

     // Close when we're done
-    function finish() {
-        s.close(); // No await, just let it finish in the background
+    async function finish() {
+        await s.close();
         res.end();

         // So that future requires don't cache our stuff, delete the whole cache
diff --git a/node_modules/nodejs-server-pages/session.js b/node_modules/nodejs-server-pages/session.js
index 54bf460..5d260d8 100644
--- a/node_modules/nodejs-server-pages/session.js
+++ b/node_modules/nodejs-server-pages/session.js
@@ -22,7 +22,20 @@ const cookie = require("cookie");
 const sqlite3 = require("sqlite3");

 function up(obj, meth) {
-    return util.promisify(obj[meth].bind(obj));
+    const raw = util.promisify(obj[meth].bind(obj));
+    return async function(...args) {
+        while (true) {
+            try {
+                return await raw(...args);
+            } catch (ex) {
+                if (ex && ex.code === "SQLITE_BUSY") {
+                    await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
+                    continue;
+                }
+                throw ex;
+            }
+        }
+    };
 }

 async function rollback(db) {
@@ -59,9 +72,20 @@ Session.prototype.init = async function(config) {

     // Make sure the database is real
     await this.run("PRAGMA journal_mode=WAL;");
-    await this.run("CREATE TABLE IF NOT EXISTS session (sid TEXT, key TEXT, value TEXT, expires TEXT);");
-    await this.run("CREATE INDEX IF NOT EXISTS session_sid ON session (sid, key);");
-    await this.run("CREATE INDEX IF NOT EXISTS session_exp ON session (expires);");
+    while (true) {
+        try {
+            await this.run("BEGIN IMMEDIATE TRANSACTION;");
+            await this.run("CREATE TABLE IF NOT EXISTS session (sid TEXT, key TEXT, value TEXT, expires TEXT);");
+            await this.run("CREATE INDEX IF NOT EXISTS session_sid ON session (sid, key);");
+            await this.run("CREATE INDEX IF NOT EXISTS session_exp ON session (expires);");
+            await this.run("COMMIT;");
+            await this.run("PRAGMA wal_checkpoint(PASSIVE);");
+            break;
+        } catch (ex) {
+            await rollback(this.db);
+            await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
+        }
+    }

     // Try to get the existing session ID
     if ("cookie" in this.request.headers) {
@@ -71,7 +95,7 @@ Session.prototype.init = async function(config) {
             sid = cookies.NJSPSESSID;

             // Check if it's actually valid
-            row = await this.dbGet("SELECT * FROM session WHERE sid=@SID;", {"@SID": sid});
+            const row = await this.dbGet("SELECT * FROM session WHERE sid=@SID;", {"@SID": sid});
             if (!row)
                 sid = null;
         }
@@ -89,7 +113,7 @@ Session.prototype.init = async function(config) {
             sid = part() + part() + part();

             try {
-                await this.run("BEGIN TRANSACTION;");
+                await this.run("BEGIN IMMEDIATE TRANSACTION;");
                 row = await this.dbGet("SELECT * FROM session WHERE sid=@SID;", {"@SID": sid});
                 if (!row) {
                     await this.run("INSERT INTO session VALUES (@SID, @KEY, @VALUE, datetime('now','" + config.expiry + " seconds'));", {
@@ -99,12 +123,14 @@ Session.prototype.init = async function(config) {
                     });
                 }
                 await this.run("COMMIT;");
+            await this.run("PRAGMA wal_checkpoint(PASSIVE);");

                 if (!row)
                     break;

             } catch (ex) {
                 await rollback(this.db);
+                await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
             }
         }
     }
@@ -150,7 +176,7 @@ Session.prototype.set = async function(key, value) {
         return;
     while (true) {
         try {
-            await this.run("BEGIN TRANSACTION;");
+            await this.run("BEGIN IMMEDIATE TRANSACTION;");
             await this.run("DELETE FROM session WHERE sid=@SID AND key=@KEY;", {
                 "@SID": this.sid,
                 "@KEY": key
@@ -161,9 +187,11 @@ Session.prototype.set = async function(key, value) {
                 "@VALUE": JSON.stringify(value)
             });
             await this.run("COMMIT;");
+            await this.run("PRAGMA wal_checkpoint(PASSIVE);");
             break;
         } catch (ex) {
             await rollback(this.db);
+            await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
         }
     }
 }
@@ -171,16 +199,38 @@ Session.prototype.set = async function(key, value) {
 Session.prototype.delete = async function(key) {
     if (!this.sid)
         return;
-    await this.run("DELETE FROM session WHERE sid=@SID AND key=@KEY;", {
-        "@SID": this.sid,
-        "@KEY": key
-    });
+    while (true) {
+        try {
+            await this.run("BEGIN IMMEDIATE TRANSACTION;");
+            await this.run("DELETE FROM session WHERE sid=@SID AND key=@KEY;", {
+                "@SID": this.sid,
+                "@KEY": key
+            });
+            await this.run("COMMIT;");
+            await this.run("PRAGMA wal_checkpoint(PASSIVE);");
+            break;
+        } catch(ex) {
+            await rollback(this.db);
+            await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
+        }
+    }
 }

 Session.prototype.cleanup = async function() {
     if (!this.sid)
         return;
-    await this.run("DELETE FROM session WHERE expires<=datetime('now');");
+    while (true) {
+        try {
+            await this.run("BEGIN IMMEDIATE TRANSACTION;");
+            await this.run("DELETE FROM session WHERE expires<=datetime('now');");
+            await this.run("COMMIT;");
+            await this.run("PRAGMA wal_checkpoint(PASSIVE);");
+            break;
+        } catch(ex) {
+            await rollback(this.db);
+            await new Promise(r => setTimeout(r, Math.floor(Math.random() * 50) + 10));
+        }
+    }
 }

 Session.prototype.close = async function() {
