const { expect } = require("chai");

describe("Ennuicastr Server Core Tests", function () {
    it("should allow a valid test to pass (placeholder)", function() {
        expect(true).to.be.true;
    });
});
